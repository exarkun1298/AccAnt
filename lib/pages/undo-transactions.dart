import 'package:flutter/material.dart';

class UndoTransactionsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "undotransactions",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
