import 'package:flutter/material.dart';

class TransactionsTemplatePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text(
        "transtemplates",
        style: TextStyle(color: Colors.white),
      ),
    );
  }
}
