import 'package:flutter/material.dart';

class FormIndentation extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      margin: EdgeInsets.symmetric(horizontal: 15),
      child: Icon(
        Icons.kitchen,
        color: Colors.grey[900],
      ),
    );
  }
}
